[pytest]
# asyncio_mode = auto means every async test function is automatically
# treated as an async test. Without this you need @pytest.mark.asyncio
# on every single test, which is tedious.
asyncio_mode = auto

# testpaths tells pytest where to look for tests
testpaths = tests

# Show short traceback for cleaner output
addopts = --tb=short
